{
  "name": "foxy-confidential-server",
  "version": "1.0.0",
  "description": "Backend server for Foxy Confidential restaurant rating app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "audit-security": "npm audit --audit-level=moderate",
    "audit-fix": "npm audit fix",
    "check-updates": "npm outdated",
    "security-check": "npm run audit-security && npm run check-env",
    "check-env": "node -e \"if (!require('fs').existsSync('.env')) { console.error('❌ .env file missing! Copy from .env.example'); process.exit(1); } else { console.log('✅ .env file found'); }\"",
    "validate-env": "node -e \"require('dotenv').config(); const required = ['JWT_SECRET', 'DB_PASSWORD', 'DB_HOST', 'DB_USER', 'DB_NAME']; const missing = required.filter(key => !process.env[key]); if (missing.length) { console.error('❌ Missing required env vars:', missing.join(', ')); process.exit(1); } else { console.log('✅ All required environment variables are set'); }\"",
    "prestart": "npm run validate-env",
    "postinstall": "npm run check-env || true"
  },
  "keywords": ["restaurant", "rating", "api", "mysql", "express"],
  "author": "Foxy Confidential Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/foxy-confidential.git"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}